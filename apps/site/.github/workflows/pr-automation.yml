# DISABLED — not executed by GitHub Actions (nested under apps/site).
# Referenced scripts (pr-monitor.ps1, etc.) and npm install are obsolete.
# Active orchestration: .github/workflows/orchestrate-issues-prs.yml

name: "[DISABLED] Site PR Automation"

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "Disabled. Use root .github/workflows/orchestrate-issues-prs.yml"
