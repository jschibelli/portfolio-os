# DISABLED — not executed by GitHub Actions
#
# Nested workflows under apps/site/.github/workflows/ are ignored by GitHub.
# This file previously targeted packages/blog-starter-kit/themes/enterprise
# (deleted). Active CI lives at:
#   .github/workflows/ci.yml
#   .github/workflows/e2e-optimized.yml
#
# Kept as a disabled stub so accidental copies to the root cannot deploy the
# obsolete blog-starter-kit path.

name: "[DISABLED] Site legacy CI/CD"

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "This workflow is intentionally disabled. Use root .github/workflows/ci.yml"
