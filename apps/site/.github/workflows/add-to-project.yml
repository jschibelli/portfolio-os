# DISABLED — not executed by GitHub Actions (nested under apps/site).
# Active workflow: .github/workflows/add-to-project.yml

name: "[DISABLED] Site Add to Project"

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "Disabled. Use root .github/workflows/add-to-project.yml"
